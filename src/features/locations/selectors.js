export const selectLoadingStatus = ({ locations }) => locations.loading;
export const locationData = ({ locations }) => locations.data;
export const dataObj = ({ locations }) => locations.dataObj;
export const configObj = ({ locations }) => locations.configObj;
