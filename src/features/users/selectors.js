export const selectLoadingStatus = ({ users }) => users.loading;
export const userData = ({ users }) => users.data;
export const dataObj = ({ users }) => users.dataObj;
export const configObj = ({ users }) => users.configObj;
